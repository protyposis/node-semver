{
  "name": "semver-upr",
  "version": "5.3.0",
  "description": "The semantic version parser used by npm (with unlocked prerelease matching npm/node-semver#192).",
  "main": "semver.js",
  "scripts": {
    "test": "tap test/*.js"
  },
  "devDependencies": {
    "tap": "^2.0.0"
  },
  "license": "ISC",
  "repository": "https://github.com/protyposis/node-semver",
  "bin": {
    "semver": "./bin/semver"
  },
  "files": [
    "bin",
    "range.bnf",
    "semver.js"
  ]
}
